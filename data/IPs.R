
number.ips.2 <- c(length(traps.ips.1$IP), length(traps.ips.2$IP), length(traps.ips.3$IP), length(traps.ips.4$IP),length(traps.ips.5$IP), length(traps.ips.6$IP), length(traps.ips.7$IP),length(traps.ips.8$IP), length(traps.ips.8$IP), length(traps.ips.10$IP))
